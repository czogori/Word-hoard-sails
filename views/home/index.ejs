<nav class="menu" id="theMenu">
    <div class="menu-wrap">
    <i class="icon-remove menu-close"></i>
     <form role="form">
        <div class="form-group">
            <input type="email" class="form-control" id="exampleInputEmail1" placeholder="Nowe słowo">
        </div>
        <button type="submit" class="btn btn-default">Dodaj słowo</button>
    </form>
    </div>

    <!-- Menu button -->
    <div id="menuToggle" class="active"><i class="icon-reorder"></i></div>
</nav>

<div class="row" ng-app="myapp">
    <div ng-controller="WordController" class="col-md-2 col-md-offset-5">
        <h1>{{ word }}</h1>
        <a ng-click="know()" href="" class="btn btn-default">Wiem</a>
        <a ng-click="dontKnow()" href="" class="btn btn-default">Nie wiem</a>
    </div>
</div>

<script type="text/javascript">
angular.module("myapp", [])
        .controller("WordController", function($scope, $http) {

            $scope.words = [];
            $scope.word = '';

            $scope.loadWords = function() {
                $http({method: 'GET', url: '/word'})
                .success(function(data, status, headers, config) {
                    console.log(data[0]);
                    $scope.words = data;
                })
                .error(function(data, status, headers, config) {
                    console.log('error');
                });
            }

            $scope.know = function() {
                console.log('know');
                $scope.word = $scope.words[Math.floor(Math.random() * $scope.words.length)].text;
            }

            $scope.dontKnow = function() {
                $scope.word = $scope.words[Math.floor(Math.random() * $scope.words.length)].text;
            }

            $scope.loadWords();

        } );
</script>
